{
  "name": "url-query-json-parser",
  "description": "Recursive parser for URL query strings to JSON objects and vice versa",
  "version": "1.0.1",
  "main": "./dist/index.js",
  "license": "MIT",
  "author": "Yoeven",
  "scripts": {
    "build": "yarn format && tsc",
    "format": "prettier --write **/*.ts"
  },
  "devDependencies": {
    "@types/node": "^17.0.23",
    "prettier": "^2.6.2",
    "ts-node": "^10.7.0",
    "tsc": "^2.0.4",
    "typescript": "^4.6.3"
  }
}
